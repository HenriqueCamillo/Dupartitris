extends Node

const BLOCKS_PER_PIECE: int = 4
const BLOCK_SIZE: float = 10

# TODO: These consts could be in a config file, but it's good enough here for now
const PIECE_SPAWN_DELAY: int = 10
const BLOCK_CLEAR_DELAY: int = 4
const STACK_DROP_DELAY: int = 4

const MAX_LEVEL: int = 29
