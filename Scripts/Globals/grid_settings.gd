extends Node

# TODO: This doesn't need to be global, can be on TGrid and passed to the pieces on spawn
const BLOCK_SIZE: int = 10
const GRID_SIZE := Vector2i(10, 20)
const INITIAL_POSITION := Vector2i(4, 0)
